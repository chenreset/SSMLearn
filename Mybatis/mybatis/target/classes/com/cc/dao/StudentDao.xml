<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.dao.StudentDao">
    <!--
        <select>:表示查询操作。里面是select语句
        id:要执行的sql语句的唯一标识，是一个自定义字符串，推荐使用dao接口的方法名称
        resultType:告诉mybatis，执行sql语句，把数据赋值给那个类型的java对象。
        resultType的值现在使用的java对象的全限定名称
    -->
    <select id="selectStudentById" resultType="com.cc.domain.Student">
        select id,name,email,age from student where id =1001
    </select>

    <select id="insertStudent">
        insert student values(1003,'李三','lisan@qq.com',22)
    </select>
</mapper>